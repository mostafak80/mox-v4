# تحديث سلة العمليات — MOX V4

## تركيب التحديث
1. فك ضغط الملف.
2. استبدل الملفات الأربعة في المستودع: `index.html` و`app.js` و`style.css` و`sw.js`.
3. ارفع التغييرات بالطريقة المعتادة وانتظر اكتمال نشر موقعك.
4. افتح الموقع، ثم أعد تحميل الصفحة بعد تفعيل تحديث الكاش. على الكمبيوتر يمكن استخدام Ctrl + Shift + R.

الملف المضغوط يحتوي على الموقع كاملًا. ملفات البيانات والشعار لم تتغير. اسم قاعدة البيانات ومخططها لم يتغيرا؛ البيانات السابقة تبقى في نفس المتصفح وعلى نفس عنوان الموقع. البيانات لا تنتقل تلقائيًا إلى عنوان جديد.

## طريقة الاستخدام
- من صفحة إضافة: اضغط على عرض من القائمة اليسار ليظهر في السلة اليمين.
- اختار 120 شدة ثم 325 شدة ثم 60 شدة: يظهر كل عرض في صف خاص به.
- زر + يزيد الشحنات، و− يقللها حتى 1. زر × يحذف العرض من السلة.
- الضغط مرة أخرى على نفس العرض يزيد شحنة بدون إنشاء صف آخر في السلة.
- يمكنك تغيير الداخل لكل شحنة في صف العرض، وإجماليات الداخل والمصروف والربح تتحدث فورًا.
- التحديد المتعدد يضيف العروض للسلة أولًا، بما فيها عروض الخدمات المختلفة.
- تاريخ الدفعة وملاحظتها يطبّقان على جميع الشحنات.
- اضغط حفظ العمليات مرة واحدة: لو عندك 5 شحنات 120 شدة وشحنة 325 وشحنة 60، النتيجة 7 صفوف مستقلة في السجل، بكمية 1 لكل صف.
- Ctrl + Enter أو Cmd + Enter اختصار للحفظ.
- السلة مؤقتة حتى الحفظ؛ إعادة تحميل الصفحة تلغي الشحنات غير المحفوظة.

## التحقق المنفذ
اختبار آلي بمتصفح Chromium: 5 شحنات لنفس العرض + عرضين آخرين، صحة الإجماليات والسعر المعدّل، هوية مستقلة لكل عملية، كتابة واحدة لقاعدة البيانات، منع الحفظ المكرر، إلغاء معاملة التخزين عمدًا ثم إعادة المحاولة، إضافة من البحث، تحديد متعدد عبر الخدمات، تفريغ وحذف، الأسماء ومعرّفات العروض ذات الرموز الخاصة، واستمرار السجل بعد إعادة التحميل.

فحص العرض على مقاسات 390 و768 و1024 و1440 بكسل، وفحص تقليل الحركة. لا أخطاء JavaScript ظهرت في الاختبار.

لتشغيل الاختبار محليًا مع Node.js:
```sh
npm install --no-save playwright
npx playwright install chromium
node tests/cashier.cjs
```
الاختبار يفتح خادمًا محليًا على المنفذ 8765، ويستخدم بيانات تجريبية في متصفح معزول. الصور الناتجة تذهب إلى test-results.

التحديث مبني على commit: f0d521ea62d9c5c6645ba4ce57f548220b8e3848.
لم يتم رفعه إلى GitHub أو نشره؛ الاتصال المتاح للقراءة فقط.
